type            	sets;
libs            	("libsampling.so");

writeControl    	writeTime;

interpolationScheme 	cellPointFace;

setFormat       	raw;

sets
(
    upperWall
    {
	type	lineUniform;
	axis	x;
	start	(-0.750 0.05 0.0);
	end	(0.970 0.05 0.0);
	nPoints	1000;
    }
);

fields          ( yPlus wallShearStress wallHeatFlux T);
